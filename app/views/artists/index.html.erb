<% @artists.each do |artist| %>
  <h2><%= artist.name %></h2>
  <p><%= artist.songs.count.to_s + ' songs' %></p>
  <ul>
  <% artist.songs.each do |song| %>
    <li><%= link_to song.title, song %></li>
  <% end %>
  </ul>
<% end %>